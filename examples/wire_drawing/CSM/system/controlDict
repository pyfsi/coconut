/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM Extend Project: Open Source CFD        |
|  \\    /   O peration     | Version:  1.6-ext                               |
|   \\  /    A nd           | Web:      www.extend-project.de                 |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      controlDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //



libs             16 ("libcoconut_solidModels.so" "libfiniteVolume.so" "libfiniteArea.so" "libmeshTools.so" "liblagrangian.so" "libfoamCalcFunctions.so" "libdynamicFvMesh.so" "libtopoChangerFvMesh.so" "libdecompositionMethods.so" "libmetisDecomp.so" "libsampling.so" "libsurfMesh.so" "libdynamicTopoFvMesh.so" "libconservativeMeshToMesh.so" "libmeshSmoothers.so" "libsolidModelsBekaert.so" );

startFrom       startTime;

startTime       0.0;

stopAt          endTime;

endTime         1e15;

deltaT          5e-05;

writeControl    timeStep;

writeInterval   1;

cycleWrite      0;

writeFormat     ascii;

writePrecision  6;

writeCompression uncompressed; 

timeFormat fixed;

timePrecision   7;

runTimeModifiable no;

adjustTimeStep	no;

functions

{
	drawingForce
	{
	 type 	solidForces;
	 historyPatch	wireDownstream;
	 stressName	sigmaCauchy;
	}

}
// ************************************************************************* //
// CoCoNuT function objects
boundary_names (wireTopContact );

functions
{
}